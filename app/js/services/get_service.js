'use strict';

module.exports = function(app) {
  app.factory('ProfileService', function($http) {
    const service = {};

    service.getUser = function(user) {

    };

    return service;
  });
};
